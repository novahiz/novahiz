## What this changes

<!-- One or two sentences. -->

## Why

<!-- The reason for the change. Link an issue when one exists. -->

## How it was tested

<!-- Commands you ran and their result. `node --test` is the baseline. -->

## Checklist

- [ ] `node --test` passes
- [ ] Tests added or updated for changes in `src/classify.ts` or `src/gate.ts`
- [ ] No machine-specific paths or secrets committed
- [ ] `catalog/` changes are called out (they shift defaults for everyone)
